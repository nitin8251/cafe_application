"""Streamlit Cloud entrypoint.

The application logic lives in app.py so local and cloud runs use the same code.
Set Streamlit Cloud's main file path to main.py if that is your deployment entry.
"""

import app  # noqa: F401
